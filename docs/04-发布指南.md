# 发布指南

## 版本号

当前版本写在：

```text
app.py -> APP_VERSION
CHANGELOG.md
README.md
README.en.md
```

发布前保持一致。

## 中文名称与搜索标题

推荐中文名称：

```text
异环光追解锁面板
```

推荐 Release 标题：

```text
异环怎么开光追：一键开启光线追踪 / 全景光追工具 v0.1.6
```

推荐关键词：

```text
异环怎么开光追，异环光追怎么开，异环全景光追怎么开，异环没有光追选项，异环光追选项不显示，异环光追打不开，异环光追开不了，异环 RTX 5060 怎么开光追，异环 RTX 4060 怎么开光追，异环光追一键开启，异环光追一键安装，异环开光追工具，异环光追补丁，NTE how to enable ray tracing，NTE no ray tracing option，NTE ray tracing fix，NTE one-click ray tracing unlock
```

## 构建

```bat
build_exe.bat
```

输出目录：

```text
dist\NTERayTracingPanel\
```

如果要做单文件版，可以手动运行：

```powershell
python -m PyInstaller --noconsole --onefile --name NTERayTracingPanel --add-data "web;web" app.py
```

## 推荐 Release 包内容

```text
NTERayTracingPanel.exe
README.md
README.en.md
NOTICE.md
LICENSE
CHANGELOG.md
run_exe_as_admin.bat
docs\
```

如果包内预置 OptiScaler，请同时保留 OptiScaler Release 自带的 `Licenses\`。

## 发布说明应写清楚

- 本工具只监听 `127.0.0.1:22642`。
- 关闭浏览器不等于退出 exe。
- 默认不修改显卡注册表。
- 每次安装都会备份。
- 需要退出时点“退出工具”或结束 `NTERayTracingPanel.exe`。

## 核心项目与致谢

发布页建议保留核心项目说明：

```text
OptiScaler:
https://github.com/optiscaler/OptiScaler

DLSSTweaks:
https://github.com/emoose/DLSSTweaks
```

同时写明边界：

```text
本项目不是 OptiScaler 本体、不是 DLSSTweaks 分支，也不是 NVIDIA 官方工具；核心实现是 OptiScaler DXGI/Streamline GPU spoof。DLSSTweaks 只作为相关项目和旧本地文件兼容备份说明，不使用其 DLSS 比例配置流程。
```
